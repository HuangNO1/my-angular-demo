import { IcUppercasePipe } from './ic-uppercase.pipe';

describe('IcUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new IcUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
